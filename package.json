{
  "name": "comps",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch": "parcel  src/style.css --dist-dir static/styles --no-hmr --no-source-maps",
    "build": "zola build && parcel build src/style.css --dist-dir static/styles --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.12",
    "axios": "^1.1.3",
    "daisyui": "^2.31.0",
    "dotenv": "^16.0.3",
    "parcel": "^2.9.3",
    "postcss": "^8.4.18",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.9"
  }
}
